# AREA config for ChartQA (chart-understanding benchmark, vision-only)

dataset_name: "chartqa"
experiment_type: "no_retrieval"

# Parquet files (glob pattern)
query_path: "$AREA_DATA_ROOT/datasets/_hf_raw/lmms-lab__ChartQA/data/test-*.parquet"

max_new_tokens: 32
K_max: 0
